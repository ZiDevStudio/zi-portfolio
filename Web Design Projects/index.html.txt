<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Web Design Projects · Zi Portfolio</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">

  <style>
    :root {
      --bg: #f5f1ec;
      --card: #ffffff;
      --text-main: #2f2b28;
      --text-muted: #857a70;
      --accent: #c8a88a;
      --border-soft: #e0d6cc;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      background: var(--bg);
      color: var(--text-main);
      min-height: 100vh;
      padding: 32px 16px;
      display: flex;
      justify-content: center;
    }

    .shell {
      max-width: 900px;
      width: 100%;
      background: var(--card);
      border-radius: 18px;
      border: 1px solid var(--border-soft);
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.04);
      padding: 28px 24px 24px;
    }

    header {
      margin-bottom: 24px;
    }

    .eyebrow {
      font-size: 0.8rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 4px;
    }

    h1 {
      font-family: "Playfair Display", serif;
      font-size: 1.8rem;
      margin-bottom: 8px;
    }

    .tagline {
      font-size: 0.95rem;
      color: var(--text-muted);
    }

    .back-link {
      font-size: 0.85rem;
      margin-bottom: 16px;
    }

    .back-link a {
      color: var(--text-muted);
      text-decoration: none;
    }

    .back-link a:hover {
      text-decoration: underline;
    }

    .projects {
      margin-top: 16px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 14px;
    }

    .project-card {
      border-radius: 12px;
      border: 1px solid var(--border-soft);
      padding: 14px 12px;
      background: #fdfaf7;
    }

    .project-card h2 {
      font-size: 1rem;
      margin-bottom: 4px;
    }

    .project-card p {
      font-size: 0.88rem;
      color: var(--text-muted);
      margin-bottom: 6px;
    }

    .project-meta {
      font-size: 0.78rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>
  <main class="shell">
    <div class="back-link">
      ← <a href="../">Back to portfolio home</a>
    </div>

    <header>
      <div class="eyebrow">Web Design</div>
      <h1>Web Design Projects</h1>
      <p class="tagline">
        A growing collection of layouts, prototypes, and live sites that blend clarity with softness.
      </p>
    </header>

    <section class="projects">
      <article class="project-card">
        <h2>Placeholder Project</h2>
        <p>A sample slot for your first real project description.</p>
        <div class="project-meta">Role: Designer · Status: In progress</div>
      </article>
    </section>
  </main>
</body>
</html>
